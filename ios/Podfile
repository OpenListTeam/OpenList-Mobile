# Uncomment this line to define a global platform for your project
platform :ios, '12.0'

# CocoaPods analytics sends network stats synchronously affecting flutter build latency.
ENV['COCOAPODS_DISABLE_STATS'] = 'true'

project 'Runner', {
  'Debug' => :debug,
  'Profile' => :release,
  'Release' => :release,
}

def flutter_root
  generated_xcode_build_settings_path = File.expand_path(File.join('..', 'Flutter', 'Generated.xcconfig'), __FILE__)
  unless File.exist?(generated_xcode_build_settings_path)
    raise "#{generated_xcode_build_settings_path} must exist. If you're running pod install manually, make sure flutter pub get is executed first"
  end

  File.foreach(generated_xcode_build_settings_path) do |line|
    matches = line.match(/FLUTTER_ROOT\=(.*)/)
    return matches[1].strip if matches
  end
  raise "FLUTTER_ROOT not found in #{generated_xcode_build_settings_path}. Try deleting Generated.xcconfig, then run flutter pub get"
end

require File.expand_path(File.join('packages', 'flutter_tools', 'bin', 'podhelper'), flutter_root)

flutter_ios_podfile_setup

target 'Runner' do
  use_frameworks!
  use_modular_headers!

  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
  
  # OpenList Framework Integration
  # Manually link xcframeworks from Frameworks directory
  target_frameworks_dir = File.expand_path('Frameworks', __dir__)
  
  if Dir.exist?(target_frameworks_dir)
    Dir.glob(File.join(target_frameworks_dir, '*.xcframework')).each do |framework_path|
      framework_name = File.basename(framework_path, '.xcframework')
      puts "Found framework: #{framework_name} at #{framework_path}"
    end
  end
end

post_install do |installer|
  installer.pods_project.targets.each do |target|
    flutter_additional_ios_build_settings(target)
  end
  
  # Configure framework search paths for all targets
  installer.pods_project.targets.each do |target|
    target.build_configurations.each do |config|
      # Ensure framework search paths include our Frameworks directory
      config.build_settings['FRAMEWORK_SEARCH_PATHS'] ||= ['$(inherited)']
      config.build_settings['FRAMEWORK_SEARCH_PATHS'] << '$(PROJECT_DIR)/Frameworks'
    end
  end
  
  # Log framework detection for debugging
  frameworks_dir = File.expand_path('Frameworks', __dir__)
  if Dir.exist?(frameworks_dir)
    frameworks = Dir.glob(File.join(frameworks_dir, '*.xcframework'))
    if frameworks.any?
      puts "✅ Found #{frameworks.size} OpenList framework(s) in Frameworks directory"
      frameworks.each { |f| puts "  - #{File.basename(f)}" }
      puts "Note: Frameworks will be linked via build settings and manual Xcode configuration"
    else
      puts "⚠️  No xcframework files found in Frameworks directory"
    end
  else
    puts "⚠️  Frameworks directory does not exist"
  end
end
